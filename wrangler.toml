# ⚠️  DO NOT USE THIS FILE FOR DEPLOYMENT.
# This file is kept only as a reference / backup.
# The active deployment config is wrangler.jsonc (Worker mode, no nonce injection).
# To deploy: wrangler deploy --config wrangler.jsonc
#
# name = "seekosint"
# pages_build_output_dir = ".open-next"   ← this caused Cloudflare Pages nonce injection
