/*
  # NOTE: Content-Security-Policy is set by middleware.ts (runs inside the Worker)
  # because Cloudflare Pages overwrites any CSP set here with its own nonce-based
  # policy. The middleware header wins over the Pages injection layer.
  #
  # Permissions-Policy is kept here as a secondary hint; it is also set by middleware.
  Permissions-Policy: browsing-topics=(), interest-cohort=()
